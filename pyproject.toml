[project]
name = "test-task-django-uptrader"
version = "0.1.0"
description = ""
authors = [
    {name = "Dmitry", email = "kiselevdv1991@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<6.0"
]

[tool.poetry]
packages = [
    {include = "config", from = "."},
    {include = "main", from = "."},
    {include = "manage.py", from = "."}
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"